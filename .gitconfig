[user]
  name  = Hayato KAWAI
  email = g@fohte.net

[color]
  ui = auto

[push]
  default = current

[alias]
  a  = add
  aa = add -A .
  ap = add -p

  br = branch

  ch = checkout
  cb = checkout -b

  c   = commit
  ca  = commit -a
  cm  = commit -m
  cam = commit -am
  cc  = commit --amend --allow-empty-message -m ''

  d  = diff
  ds = diff --staged

  f = fetch -p

  l  = log --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --graph --abbrev-commit --date=relative -n 15 --branches

  m  = merge

  s = status

  ss = stash save
  sp = stash pop

  p = push

  pl = pull

  rb = rebase

  rs = reset --soft
  rh = reset --hard

[merge]
  ff = false
