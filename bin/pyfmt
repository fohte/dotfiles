#!/bin/bash

dir=${1:-.}
files=$(find $dir -name '*.py')
for file in $files; do
  if [ "$(basename $file)" = '__init__.py' ]; then
    continue
  fi
  autoflake --remove-all-unused-imports -i $file
done

isort -y -q $files
autopep8 -i $files
